# -*- coding: utf-8 -*-
# Per-page SEO metadata catalog.
# Each entry is keyed by URL path and provides the canonical neutral-resource
# title, description, og_image (relative or absolute), and optional template
# metadata (article_pubdate, article_modified, language).
#
# Pages not in this catalog will keep their existing title and description so
# we never silently overwrite hand-tuned copy.

CATALOG = {}
